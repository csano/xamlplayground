<Window x:Class="XamlPlayground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:XamlPlayground"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <vm:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter" />
        <vm:MainWindowViewModel x:Key="MainWindowViewModel" />
    </Window.Resources>
    <Grid>
        <Button x:Name="button1" Content="Button" HorizontalAlignment="Left" Margin="10,22,0,0" VerticalAlignment="Top" Width="131" Visibility="{Binding IsButtonVisible, Converter={StaticResource BoolToVisConverter}}" Height="55" />
        <Button x:Name="button2" Content="Button" HorizontalAlignment="Left" Margin="10,122,0,0" VerticalAlignment="Top" Width="131" Visibility="{Binding Path=IsButtonVisible, Source={StaticResource MainWindowViewModel}, Converter={StaticResource InverseBoolToVisConverter}}" Height="55" />
    </Grid>
</Window>
